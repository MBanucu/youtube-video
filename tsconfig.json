{
  "compilerOptions": {
    // ───────────────────────────────────────
    // Environment & modern JS/TS features
    // ───────────────────────────────────────
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "Preserve",               // ← good choice with Bun
    "moduleDetection": "force",
    "jsx": "react-jsx",
    "allowJs": true,

    // ───────────────────────────────────────
    // Bundler-friendly mode (Bun loves this)
    // ───────────────────────────────────────
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,                     // you're not using tsc to emit → perfect

    // ───────────────────────────────────────
    // Path aliases — the main addition
    // ───────────────────────────────────────
    "baseUrl": ".",                     // ← important: makes paths relative to project root
    "paths": {
      "@/*": ["src/*"],                  // now you can write: import { … } from "@/utils"
      "@test/*": ["test/*"]
    },

    // ───────────────────────────────────────
    // Strictness & best practices
    // ───────────────────────────────────────
    "strict": true,
    "skipLibCheck": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    // Optional stricter flags (you had them off → kept as-is)
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noPropertyAccessFromIndexSignature": false
  }
}
